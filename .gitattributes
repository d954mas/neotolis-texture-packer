*.ntpacker_project text eol=lf
*.golden text eol=lf
*.expected.json text eol=lf

# U-01 owner-scale bench assets (real Kenney CC0 sprites). Stored in Git LFS so
# the base clone stays lean; the deterministic manifest below is plain text.
examples/bench-assets/**/*.png filter=lfs diff=lfs merge=lfs -text
examples/bench-assets/manifest.tsv text eol=lf
